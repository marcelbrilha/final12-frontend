import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  footer: {
    fontFamily: "Roboto",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: "14px",
    color: "#333",
    padding: "10px 0",
    marginTop: "50px",
  },
});
